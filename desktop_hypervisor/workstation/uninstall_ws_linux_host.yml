# Copyright 2023-2026 VMware, Inc.
# SPDX-License-Identifier: BSD-2-Clause
---
- name: "Stop VMs if running before uninstall_workstation"
  include_tasks: ../common/dh_host_stop_vms.yml

- name: "Uninstall Workstation on {{ host_machine_hostname }}"
  block:
    - name: "Uninstall Workstation if it exists"
      ansible.builtin.shell: "sudo vmware-installer --required -u vmware-workstation 2>&1 | tee -a /tmp/vmware-uninstaller.log"
      delegate_to: "{{ host_machine_hostname }}"
      register: ws_uninstall_result
      failed_when: >
        'error' in ws_uninstall_result.stdout.lower() or
        'failed' in ws_uninstall_result.stdout.lower() or
        'warning' in ws_uninstall_result.stdout.lower()
  always:
    - name: "Get the vmware-uninstaller log to local"
      include_tasks: ../common/dh_get_file.yml
      vars:
        file_src_path: "/tmp/vmware-uninstaller.log"
        file_dst_path: "{{ current_test_log_folder }}"

- name: "Display Workstation silent install result"
  debug: var=ws_uninstall_result

- name: "Check Workstation installation status in Linux host"
  include_tasks: ../common/lin_host_check_workstation.yml

- name: "Assert Workstation is uninstalled"
  ansible.builtin.assert:
    that:
      - dh_host_installed is defined and not dh_host_installed
    fail_msg: "Failed to uninstall Workstation"
